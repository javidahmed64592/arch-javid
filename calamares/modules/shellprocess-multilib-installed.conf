---
# Runs on the HOST live system after pacstrap.
# Enables multilib in the INSTALLED system's pacman.conf.
# Pacstrap creates a fresh pacman.conf in the target that does not have multilib.
dontChroot: true
timeout: 30
script:
  - "/etc/calamares/scripts/multilib-installed.sh @@ROOT@@"
