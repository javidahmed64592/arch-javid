---
# Runs on the HOST live system before pacstrap.
# Enables the multilib repository so lib32-* packages in packages.txt can resolve.
dontChroot: true
timeout: 120
script:
  - "/etc/calamares/scripts/multilib-live.sh"
