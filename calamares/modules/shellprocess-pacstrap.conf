---
# Runs on the HOST live system.
# Installs all packages from packages.txt into the Calamares target root via pacstrap.
dontChroot: true
timeout: 3600
script:
  - "/etc/calamares/scripts/pacstrap.sh"
