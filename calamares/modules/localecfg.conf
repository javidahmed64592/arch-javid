---
# Ensures en_GB.UTF-8 is uncommented in locale.gen and locale-gen is run.
# The locale actually written to locale.conf is driven by the user's selection in the wizard.
locales:
  - "en_GB.UTF-8 UTF-8"
