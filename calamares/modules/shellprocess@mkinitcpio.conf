---
# Rebuild the initramfs inside the installed system chroot.
# Runs after fstab and users are configured, before the bootloader is set up.
dontChroot: false
timeout: 120

script:
  - command: "/usr/bin/mkinitcpio -P"
    timeout: 120
