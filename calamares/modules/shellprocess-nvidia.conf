---
# Configures NVIDIA driver for the installed system.
# Writes modprobe options and adds NVIDIA modules to mkinitcpio.conf.
# MUST run before shellprocess@initramfs (mkinitcpio reads mkinitcpio.conf).
dontChroot: true
timeout: 60
script:
  - "/etc/calamares/scripts/nvidia.sh @@ROOT@@"
