---
# Override the default shellprocess.conf shipped by the calamares package
# (which contains a placeholder "slowloris" command).
# This file is intentionally empty — all shell commands are defined in
# shellprocess@mkinitcpio.conf instead.
dontChroot: false
script: []
