---
# Packages to operate on inside the installed system (chroot).
#
# "remove" entries are packages that only belong on the live ISO
# and should not end up in the final installed system.
#
# Add any "install" entries here if extra packages beyond the live
# snapshot are desired.

backend: pacman

operations:
    - remove:
        - calamares
        - calamares-tools
