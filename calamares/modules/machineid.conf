---
# Configuration for the machineid module, which creates /etc/machine-id in the target system.

systemd: true
systemd-style: uuid

dbus: true
dbus-symlink: true

entropy-copy: false
entropy-files:
    - /var/lib/urandom/random-seed
    - /var/lib/systemd/random-seed
